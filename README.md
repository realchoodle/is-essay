# Readme File

This repository houses the first draft of my IS paper. The three most relevant
file extensions are `.tex`, which is the raw LaTeX code; `.bib`, which contains
my BibTeX bibliography; and `.pdf`, which contains the rendered PDF file.
